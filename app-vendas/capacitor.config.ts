import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.appvendas',
  appName: 'app-vendas',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
